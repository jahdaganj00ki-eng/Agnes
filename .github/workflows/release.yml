name: Release

on:
  push:
    tags: ['v*.*.*']
  workflow_dispatch:

jobs:
  build-and-package:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x
      - run: pwsh scripts/extract-skills.ps1
      - run: dotnet restore AgnesWindows.sln
      - run: dotnet build src/AgnesWindows.Core/AgnesWindows.Core.csproj src/AgnesWindows.Infrastructure/AgnesWindows.Infrastructure.csproj tests/AgnesWindows.Tests/AgnesWindows.Tests.csproj --configuration Release --no-restore
      - run: dotnet test tests/AgnesWindows.Tests/AgnesWindows.Tests.csproj --no-build --verbosity normal
      - name: Build UI/App (may fail on GitHub runners)
        run: |
          dotnet build src/AgnesWindows.UI/AgnesWindows.UI.csproj src/AgnesWindows.App/AgnesWindows.App.csproj --configuration Release --no-restore || echo "UI build failed (expected on GitHub runners), continuing..."
      - run: dotnet publish src/AgnesWindows.App/AgnesWindows.App.csproj --configuration Release --runtime win-x64 --self-contained true -p:PublishSingleFile=true -o publish/x64 || echo "Publish x64 failed (expected on GitHub runners)"
      - run: dotnet publish src/AgnesWindows.App/AgnesWindows.App.csproj --configuration Release --runtime win-arm64 --self-contained true -p:PublishSingleFile=true -o publish/arm64 || echo "Publish arm64 failed (expected on GitHub runners)"
      - name: Create MSIX
        run: dotnet msbuild src/AgnesWindows.App/AgnesWindows.App.wapproj /p:Configuration=Release /p:Platform=x64 /p:AppxBundle=Always || echo "MSIX creation failed (expected on GitHub runners)"
      - name: Sign MSIX
        if: secrets.CERTIFICATE_BASE64 != ''
        run: |
          $certPath = "$env:RUNNER_TEMP\AgnesWindows.pfx"
          [System.IO.File]::WriteAllBytes($certPath, [Convert]::FromBase64String($env:CERTIFICATE_BASE64))
          & signtool sign /f $certPath /p $env:CERTIFICATE_PASSWORD /fd SHA256 /tr http://timestamp.digicert.com /td SHA256 "src\AgnesWindows.App\AppPackages\AgnesWindows_*.msix"
      - uses: actions/upload-artifact@v4
        with:
          name: release-artifacts
          path: |
            publish/x64/
            publish/arm64/
            src/AgnesWindows.App/AppPackages/
      - uses: softprops/action-gh-release@v1
        with:
          files: |
            publish/x64/AgnesWindows.exe
            publish/arm64/AgnesWindows.exe
            src/AgnesWindows.App/AppPackages/*.msix
          generate_release_notes: true