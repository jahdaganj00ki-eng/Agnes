<Window
    x:Class="AgnesWindows.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:AgnesWindows.UI.ViewModels"
    xmlns:views="using:AgnesWindows.UI.Views"
    Title="Agnes Windows"
    MinWidth="800"
    MinHeight="600"
    Width="1200"
    Height="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Navigation -->
        <NavigationView
            Grid.Row="0"
            PaneDisplayMode="Top"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            Background="{StaticResource CardBackgroundBrush}">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Edit Image" IsSelected="True" Tag="edit"/>
                <NavigationViewItem Content="History" Tag="history"/>
                <NavigationViewItem Content="Settings" Tag="settings"/>
            </NavigationView.MenuItems>
        </NavigationView>

        <!-- Main Content Area -->
        <ScrollViewer 
            Grid.Row="1" 
            VerticalScrollBarVisibility="Auto"
            Padding="24">
            <StackPanel Spacing="16">
                <TextBlock 
                    Text="Edit Image" 
                    FontSize="28" 
                    FontWeight="SemiBold"
                    Foreground="{StaticResource TextPrimaryBrush}"/>

                <views:ChatView 
                    HorizontalAlignment="Stretch"
                    Height="400"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Input Bar -->
        <Border 
            Grid.Row="2" 
            Background="{StaticResource CardBackgroundBrush}"
            Padding="16">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox
                    Grid.Column="0"
                    PlaceholderText="Describe your edit..."
                    BorderBrush="Transparent"
                    Background="#2C2C2E"
                    CornerRadius="8"
                    Padding="12,8"
                    Text="{Binding UserPrompt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <Button
                    Grid.Column="1"
                    Content="📎"
                    Width="48"
                    Height="48"
                    CornerRadius="24"
                    Background="{StaticResource AccentBlueBrush}"
                    Click="OnAttachImageClick"/>

                <Button
                    Grid.Column="2"
                    Content="Send"
                    Width="80"
                    Height="48"
                    CornerRadius="24"
                    Background="{StaticResource AccentCyanBrush}"
                    Click="OnSendClick"/>
            </Grid>
        </Border>
    </Grid>
</Window>
