<UserControl
    x:Class="AgnesWindows.UI.Views.ImageBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:AgnesWindows.UI.ViewModels"
    MinHeight="300">

    <Border 
        Background="#1A1A1C" 
        CornerRadius="8"
        Padding="16">
        <StackPanel Spacing="12">
            <TextBlock 
                Text="Generated Image" 
                Foreground="{StaticResource TextPrimaryBrush}"
                FontSize="14"
                FontWeight="SemiBold"/>

            <Border 
                CornerRadius="8"
                Height="300"
                Background="#2C2C2E">
                <Image 
                    Source="{Binding LastResult.ImageUrl}" 
                    Stretch="Uniform"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>
            </Border>

            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                <Button Content="👍" Width="40" Height="40" Background="Transparent"/>
                <Button Content="👎" Width="40" Height="40" Background="Transparent"/>
                <Button Content="📋" Width="40" Height="40" Background="Transparent"/>
                <Button Content="↗️" Width="40" Height="40" Background="Transparent"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
