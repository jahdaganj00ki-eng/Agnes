<UserControl
    x:Class="AgnesWindows.UI.Views.PromptEnhancementPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:AgnesWindows.UI.ViewModels"
    MinHeight="100">

    <Border 
        Background="#1A1A1C" 
        CornerRadius="8"
        Padding="16">
        <StackPanel Spacing="8">
            <TextBlock 
                Text="Prompt Enhancement" 
                Foreground="{StaticResource AccentCyanBrush}"
                FontSize="14"
                FontWeight="SemiBold"/>

            <StackPanel Spacing="4">
                <TextBlock 
                    Text="Original:" 
                    Foreground="{StaticResource TextSecondaryBrush}"
                    FontSize="12"/>
                <TextBlock 
                    Text="{Binding UserPrompt}" 
                    Foreground="{StaticResource TextSecondaryBrush}"
                    TextWrapping="Wrap"
                    FontStyle="Italic"/>
            </StackPanel>

            <StackPanel Spacing="4">
                <TextBlock 
                    Text="Enhanced:" 
                    Foreground="{StaticResource TextPrimaryBrush}"
                    FontSize="12"
                    FontWeight="Medium"/>
                <TextBlock 
                    Text="{Binding EnhancedPrompt}" 
                    Foreground="{StaticResource TextPrimaryBrush}"
                    TextWrapping="Wrap"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
